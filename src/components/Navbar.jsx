export default function Navbar() {
    return (
      <nav style={{ backgroundColor: 'var(--vino-oscuro)', padding: 'var(--espaciado)', borderRadius: 'var(--radio)', marginBottom: 'var(--espaciado)' }}>
        <h2 className="titulo" style={{ color: 'var(--blanco-calido)' }}>Mi Portafolio</h2>
      </nav>
    );
  }